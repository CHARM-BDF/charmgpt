{
    "name": "charm-mcp",
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "start": "concurrently -n \"VITE,SERVER\" -c \"blue,green\" \"npm run dev --workspace=frontend-client\" \"npm run server:dev --workspace=backend-mcp-client\"",
        "build:mcp-servers": "for dir in custom-mcp-servers/*; do if [ -f \"$dir/package.json\" ]; then (cd \"$dir\" && echo \"Building $dir...\" && npm run build); fi; done",
        "dev:mcp-servers": "for dir in custom-mcp-servers/*; do if [ -f \"$dir/package.json\" ]; then (cd \"$dir\" && echo \"Starting $dir...\" && npm run dev); fi; done",
        "install:mcp-servers": "for dir in custom-mcp-servers/*; do if [ -f \"$dir/package.json\" ]; then (cd \"$dir\" && echo \"Installing dependencies for $dir...\" && npm install); fi; done",
        "build:all": "npm run build:mcp-servers && npm run build",
        "test:system": "npm run test:e2e --workspace=system-tests"
    },
    "devDependencies": {
        "concurrently": "^9.2.0"
    },
    "workspaces": [
        "backend-mcp-client",
        "frontend-client",
        "shared",
        "prototype",
        "system-tests"
    ],
    "dependencies": {
        "@codemirror/lang-javascript": "^6.2.4",
        "@codemirror/lang-python": "^6.2.1",
        "@codemirror/language": "^6.11.3",
        "@codemirror/legacy-modes": "^6.5.1",
        "@codemirror/theme-one-dark": "^6.1.3",
        "@codemirror/view": "^6.38.2",
        "@modelcontextprotocol/sdk": "^1.18.1",
        "@uiw/react-codemirror": "^4.25.2",
        "papaparse": "^5.5.3",
        "xmldom": "^0.6.0"
    }
}
