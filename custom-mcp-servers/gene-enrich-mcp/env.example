# Example Environment Configuration for Gene Enrichment MCP
# Copy this file to .env and replace the placeholder values

# API Configuration - GeLiNEA TRAPI endpoints
# Use development server by default (public, no auth required)
GENE_ENRICH_BASE_URL=https://translator.broadinstitute.org/gelinea-trapi/v1.5

# Alternative servers (uncomment to use):
# GENE_ENRICH_BASE_URL=https://molepro-gelinea-trapi.test.transltr.io/gelinea-trapi/v1.5  # Test server
# GENE_ENRICH_BASE_URL=https://molepro-gelinea-trapi.ci.transltr.io/gelinea-trapi/v1.5   # Staging server

# No API key required - this is a public TRAPI endpoint
# GENE_ENRICH_API_KEY=not_required

# Optional Configuration
# GENE_ENRICH_RATE_LIMIT_MS=2000               # Rate limiting between requests (2 seconds)
# GENE_ENRICH_TIMEOUT_MS=120000                # Request timeout in milliseconds (2 minutes)
# GENE_ENRICH_BATCH_SIZE_LIMIT=2500            # Max genes per request

# Development Settings
# LOG_LEVEL=debug                              # Set to 'info' or 'error' for production
# NODE_ENV=development                         # Set to 'production' for deployment

# Notes:
# - This is a public TRAPI endpoint, no authentication required
# - Gene enrichment analysis can take time, so timeout is set to 2 minutes
# - Batch size limit is 2500 genes per request as per TRAPI specification
# - Never commit the actual .env file to version control 