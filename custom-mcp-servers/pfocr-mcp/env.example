# PFOCR MCP Server Environment Variables

# Optional: User email for API usage tracking
PFOCR_USER_EMAIL=your-email@example.com

# Optional: API key if required (currently PFOCR API is public)
# PFOCR_API_KEY=your_api_key_here

# Optional: Rate limiting (milliseconds between requests)
# PFOCR_RATE_LIMIT_MS=1000

# Optional: Request timeout (milliseconds)
# PFOCR_TIMEOUT_MS=30000

# Optional: Default server to use (production, testing, staging)
# PFOCR_SERVER=production
