# Example Environment Configuration for DGIdb MCP
# Copy this file to .env and replace the placeholder values

# API Configuration - BioThings DGIdb
# Use production server by default (public, no auth required)
DGIDB_BASE_URL=https://biothings.ncats.io/dgidb

# Alternative servers (uncomment to use):
# DGIDB_BASE_URL=https://biothings.test.transltr.io/dgidb  # Test server
# DGIDB_BASE_URL=https://biothings.ci.transltr.io/dgidb   # Staging server

# No API key required - this is a public BioThings API
# DGIDB_API_KEY=not_required

# Optional Configuration
# DGIDB_RATE_LIMIT_MS=1000                      # Rate limiting between requests (1 second)
# DGIDB_TIMEOUT_MS=30000                        # Request timeout in milliseconds (30 seconds)
# DGIDB_MAX_BATCH_SIZE=1000                     # Max associations per batch request

# Development Settings
# LOG_LEVEL=debug                              # Set to 'info' or 'error' for production
# NODE_ENV=development                         # Set to 'production' for deployment

# Notes:
# - This is a public BioThings API, no authentication required
# - Supports comprehensive drug-gene interaction data
# - Includes detailed interaction types (inhibitor, agonist, antagonist, etc.)
# - Batch size limit is 1000 associations per request
# - Never commit the actual .env file to version control 