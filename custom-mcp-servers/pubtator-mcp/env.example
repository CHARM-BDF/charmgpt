# PubTator MCP Environment Configuration
# Copy this file to .env and replace the placeholder values

# PubTator API Configuration
PUBTATOR_BASE_URL=https://www.ncbi.nlm.nih.gov/research/pubtator3-api
PUBTATOR_API_KEY=your_pubtator_api_key_here
PUBTATOR_USER_EMAIL=your.email@example.com

# Optional Configuration
# PUBTATOR_RATE_LIMIT_MS=2000                   # Rate limiting between requests (2 seconds recommended)
# PUBTATOR_TIMEOUT_MS=60000                     # Request timeout in milliseconds
# PUBTATOR_MAX_RETRIES=3                        # Maximum retry attempts for failed requests
# PUBTATOR_MAX_BATCH_SIZE=100                   # Maximum number of PMIDs per batch request
# PUBTATOR_ASYNC_POLL_INTERVAL_MS=5000          # Polling interval for async text annotation (5 seconds)
# PUBTATOR_ASYNC_MAX_WAIT_MS=300000             # Maximum wait time for async operations (5 minutes)

# Development Settings
# LOG_LEVEL=debug                              # Set to 'info' or 'error' for production
# NODE_ENV=development                         # Set to 'production' for deployment

# Notes:
# - PubTator API is free but requires registration for API key
# - Rate limiting is important to avoid being blocked
# - Async text annotation can take several minutes for large texts
# - Never commit the actual .env file to version control 