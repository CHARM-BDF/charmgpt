# Example Environment Configuration for TTD MCP
# Copy this file to .env and replace the placeholder values

# API Configuration - BioThings Therapeutic Target Database
# Use production server by default (public, no auth required)
TTD_BASE_URL=https://biothings.ncats.io/ttd

# Alternative servers (uncomment to use):
# TTD_BASE_URL=https://biothings.test.transltr.io/ttd  # Test server
# TTD_BASE_URL=https://biothings.ci.transltr.io/ttd   # Staging server

# No API key required - this is a public BioThings API
# TTD_API_KEY=not_required

# Optional Configuration
# TTD_RATE_LIMIT_MS=1000                      # Rate limiting between requests (1 second)
# TTD_TIMEOUT_MS=30000                        # Request timeout in milliseconds (30 seconds)
# TTD_MAX_BATCH_SIZE=1000                     # Max associations per batch request

# Development Settings
# LOG_LEVEL=debug                              # Set to 'info' or 'error' for production
# NODE_ENV=development                         # Set to 'production' for deployment

# Notes:
# - This is a public BioThings API, no authentication required
# - Supports drug-disease, target-disease, drug-protein target associations
# - Batch size limit is 1000 associations per request
# - Never commit the actual .env file to version control 